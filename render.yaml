services:
  - type: web
    name: url-shortener
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: REDIS_URL
        value: redis://url-shortener-redis.internal:6379/0
      - key: BASE_URL
        value: https://url-shortener.onrender.com
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1

  - type: redis
    name: url-shortener-redis
    plan: free
    ipAllowList: [] 